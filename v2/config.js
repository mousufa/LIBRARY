window.LIBRARY_SUPABASE_CONFIG = {
  // Fill these after creating the Supabase project.
  // The app stays in local fallback mode until both values are set.
  url: "",
  anonKey: "",
  table: "library_records",
};
